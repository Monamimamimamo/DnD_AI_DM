# 🎮 AI Dungeon Master (Java)

Автономная замена Dungeon Master для настольной ролевой игры D&D 5e с локальной моделью Mistral 7B.

**Проект переведен на Java!** См. [README_JAVA.md](README_JAVA.md) для подробностей.

## 🚀 Описание

Этот проект создает AI-систему, которая может заменить человека в роли Dungeon Master, генерируя сюжеты, управляя NPC и следя за соблюдением правил D&D. **Работает полностью локально без API ключей!**

## ✨ Особенности

- **Мультиагентная архитектура** - разделение ролей между компонентами
  - **DM Agent** - нарратив и атмосфера (LLM, рекомендуется Mistral 7B)
  - **Rule Engine** - детерминированная проверка правил D&D 5e с использованием **полных данных SRD**
  - **Action Parser** - интерпретация любых действий игрока (LLM, рекомендуется Llama 3.1 8B)
- **Локальные модели** - работает без интернета и API ключей
  - Разные модели для разных задач (творческая для DM, детерминированная для парсинга)
- **Полная поддержка D&D 5e** - все классы, расы, навыки и правила
- **Генерация сюжетов** - атмосферные описания и интерактивные диалоги
- **Сохранение игры** - можно продолжать приключения позже
- **Бесплатно** - никаких ограничений по использованию

## 🛠️ Установка

### 1. Установка Java и Gradle

```bash
# Java 17 или выше
sudo apt install openjdk-17-jdk

# Gradle (опционально, можно использовать Gradle Wrapper)
sudo apt install gradle
# Или используйте Gradle Wrapper: ./gradlew
```

### 2. Установка Ollama

```bash
curl -fsSL https://ollama.ai/install.sh | sh
```

### 3. Загрузка моделей

**Для DM Agent (нарратив, творчество):**
```bash
ollama pull mistral:7b
```

**Для Action Parser (парсинг действий в JSON):**
```bash
ollama pull llama3.1:8b
```

### 4. Запуск 5e-srd-api

```bash
cd 5e-srd-api
docker compose up -d
```

API будет доступен по адресу: `http://localhost:3000/api/2014`

### 5. Сборка проекта

```bash
# Используя Gradle Wrapper (рекомендуется)
./gradlew build

# Или если Gradle установлен глобально
gradle build
```

## 🎮 Использование

### HTTP API Сервер (рекомендуется)

```bash
# Запуск API сервера
./gradlew run

# Сервер запустится на http://localhost:8080
# См. API_DOCUMENTATION.md для полной документации API
```

### Консольный режим (legacy)

```bash
# Запуск консольного приложения
java -cp build/libs/ai-dungeon-master-1.0.0.jar com.dnd.Main
```

**Примечание:** При первом запуске Gradle может занять некоторое время для загрузки зависимостей. Настройки памяти уже настроены в `build.gradle` и `gradle.properties`.

### Быстрый старт с API

```bash
# 1. Запустить сервер
./gradlew run

# 2. В другом терминале - начать кампанию
curl -X POST http://localhost:8080/api/campaign/start \
  -H "Content-Type: application/json" \
  -d '{}'

# 3. Добавить персонажа
curl -X POST http://localhost:8080/api/character \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Арагорн",
    "class": "FIGHTER",
    "race": "HUMAN",
    "level": 3
  }'

# 4. Выполнить действие
curl -X POST http://localhost:8080/api/action \
  -H "Content-Type: application/json" \
  -d '{
    "action": "осмотреться",
    "character_name": "Арагорн"
  }'
```

## 📁 Структура проекта

```
src/main/java/com/dnd/
├── game_state/          # Состояние игры
│   ├── Character.java
│   ├── GameState.java
│   └── GameManager.java
├── game_rules/          # Правила D&D 5e
│   ├── DiceRoller.java
│   ├── RuleEngine.java
│   ├── ActionParser.java
│   └── SRDDataLoader.java
├── ai_engine/           # AI движок
│   ├── LocalLLMClient.java
│   ├── GameOrchestrator.java
│   └── DungeonMasterAI.java
└── Main.java            # Точка входа
```

## 📝 Примечания

- **5e-srd-api** - отдельный Node.js сервис (не изменяется)
- **Ollama** - должен быть запущен на localhost:11434
- **База данных** - 

## 🔄 Миграция с Python

Проект был переведен с Python на Java. См. [JAVA_MIGRATION_SUMMARY.md](JAVA_MIGRATION_SUMMARY.md) для подробностей.
